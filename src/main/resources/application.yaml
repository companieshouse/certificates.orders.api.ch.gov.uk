server:
  servlet:
    context-path: /orderable

uk:
  gov:
    companieshouse:
      items:
        orders:
          api:
            path: /certificates

spring:
  data:
    mongodb:
      uri: ${MONGODB_URL}
